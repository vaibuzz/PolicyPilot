# VITE_API_URL is set at build time by Vercel.
# For local development, this file makes the frontend point to localhost.
# In production, set VITE_API_URL in Vercel Dashboard → Project Settings → Environment Variables.
VITE_API_URL=http://localhost:8000
